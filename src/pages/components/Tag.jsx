const Tag = ({label}) => {

    return (
        <span className="bg-red-400 text-white text-xs font-semibold px-6 py-1 rounded-[10px] mr-2 mb-2">
      {label}
    </span>
    );
};

export default Tag;
